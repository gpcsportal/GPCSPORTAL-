@extends('admin.layout') @section('title','Account') @section('content')<h1>Admin Account Settings</h1><div class="card"><form method="POST" action="{{ route('admin.account.update') }}">@csrf @method('PUT')<label>Admin ID<input name="admin_identifier" value="{{ old('admin_identifier',auth()->user()->admin_identifier) }}" required></label><label>Email<input type="email" name="email" value="{{ old('email',auth()->user()->email) }}" required></label><label>Current Password<input type="password" name="current_password" required></label><label>New Password, optional<input type="password" name="password" minlength="12"></label><label>Confirm New Password<input type="password" name="password_confirmation" minlength="12"></label><button type="submit">Update Account</button></form></div>@endsection
