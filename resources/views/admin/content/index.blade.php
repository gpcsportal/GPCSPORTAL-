@extends('admin.layout') @section('title',ucfirst($type)) @section('content')<h1>{{ ucfirst($type) }} Moderation</h1><div class="card"><table><tr><th>ID</th><th>Details</th><th>Status</th><th>Moderation</th><th>Delete</th></tr>@forelse($items as $item)<tr><td>{{ $item->id }}</td><td>@if($type==='papers'){{ $item->paper_name ?: $item->original_name }} — {{ $item->branch }} {{ $item->semester }}@elseif($type==='notes'){{ $item->title ?: $item->subject_name }} — {{ $item->branch }} {{ $item->semester }}@elseif($type==='gallery'){{ $item->category }} — {{ $item->original_name }}@else{{ $item->name }} — {{ $item->contact }}<br>{{ $item->message }}@endif</td><td>{{ $item->status }}</td><td>@if($type!=='messages')<form method="POST" action="{{ route('admin.content.status',[$type,$item->id]) }}">@csrf @method('PATCH')<select name="status"><option>pending</option><option>approved</option><option>rejected</option></select><label>Reason<textarea name="rejection_reason"></textarea></label><button type="submit">Save</button></form>@endif</td><td><form method="POST" action="{{ route('admin.content.destroy',[$type,$item->id]) }}" onsubmit="return confirm('Delete this record?')">@csrf @method('DELETE')<button class="danger" type="submit">Delete</button></form></td></tr>@empty<tr><td colspan="5">No records.</td></tr>@endforelse</table>{{ $items->links() }}</div>@endsection
