@extends('admin.layout') @section('title','Users') @section('content')<h1>Users</h1><div class="card"><table><tr><th>Name</th><th>Login</th><th>Role</th><th>Status</th><th>Action</th></tr>@forelse($users as $u)<tr><td>{{ $u->name }} {{ $u->surname }}</td><td>{{ $u->email }}<br><span class="muted">{{ $u->mobile }}</span></td><td>{{ ucfirst($u->role) }}</td><td><span class="status">{{ $u->is_active?'Active':'Suspended' }}</span></td><td><form method="POST" action="{{ route('admin.users.toggle',$u) }}">@csrf @method('PATCH')<button type="submit">{{ $u->is_active?'Suspend':'Activate' }}</button></form></td></tr>@empty<tr><td colspan="5">No users yet.</td></tr>@endforelse</table>{{ $users->links() }}</div>@endsection
